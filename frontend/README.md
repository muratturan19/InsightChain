# InsightChain Frontend

Bu dizin React ve Vite kullanılarak oluşturulan dashboard arayüzünü içerir. Tailwind CSS ile kurumsal renk şeması kullanılır.

## Çalıştırma

```bash
npm install
npm run dev
```
